// import { useLoaderData } from "react-router-dom";

const Postduplicate = () => {
    // const userInfo = useLoaderData()
    // console.log(userInfo)
    return (
        <div>
           this is my post 
        </div>
    );
};

export default Postduplicate;